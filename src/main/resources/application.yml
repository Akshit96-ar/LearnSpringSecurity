spring:
  application:
    name: LearnSpringSecurityApplication

# <---hardcoded Userdetails---->
#<------ no needed when using db authentication ---->
  security:
    user:
      name: akshit
      password: password

  datasource:
    url: jdbc:postgresql://localhost:5444/learnspring
    username: postgres
    password: postgres